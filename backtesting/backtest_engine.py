import pandas as pd\nimport numpy as np\n\nclass BacktestEngine:\n    def __init__(self, data):\n        self.data = data\n        self.results = []\n\n    def run_backtest(self, strategy):\n        for index in range(len(self.data)):\n            if strategy.should_buy(self.data.iloc[index]):\n                self.results.append(self.data.iloc[index])\n            elif strategy.should_sell(self.data.iloc[index]):\n                self.results.append(-self.data.iloc[index])\n\n    def get_results(self):\n        return self.results\n\n# Example strategy (to be defined by the user)\nclass ExampleStrategy:\n    @staticmethod\n    def should_buy(data):\n        return data['signal'] == 'buy'\n\n    @staticmethod\n    def should_sell(data):\n        return data['signal'] == 'sell'\n\n# Usage\n# data = pd.read_csv('path/to/historical_data.csv')  # Load your data\n# backtest_engine = BacktestEngine(data)\n# backtest_engine.run_backtest(ExampleStrategy)\n# results = backtest_engine.get_results()